#getComputerChoice
GENERATE a random string from "rock"|"paper"|"scissors"
RETURN the string

#getPlayerChoice
GET user input
NORMALIZE user input
RETURN user input

#playRound
GET playerSelection
GET computerSelection
IF playerSelection is not from among paper, rock nor scissors
    RETURN an error message
ELSE IF playerSelection is equal computerSelection
    RETURN a draw message
ELSE IF playerSelection beats computerSelection
    RETURN a win message
ELSE
    RETURN a lose message

#playGame
CREATE score tracking variables
FOR 5 cycles
    CALL playRound
    ADD result to score
IF player has the same score as the computer
    RETURN a draw message
ELSE IF player has higher score than the computer
    RETURN a win message
ELSE
    RETURN a lose message